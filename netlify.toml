[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# Optional: Configure redirects for single-page applications
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200